<div class="bordered_wrapper" style="overflow:auto;">
  <div class="header_info_wrapper">
    <div class="span2 header_image">
      <%= image_tag @book.cover.url(:large) %>
    </div>

    <div class="span8">
      <h3>
        <%= @book.title %> by <%= @book.author %> <%= link_to 'Edit Book', edit_book_path(@book), :class => 'btn btn-inverse pull-right' %>
      </h3>
      <p>
        <b>Description:</b>
        <%= @book.description %>
      </p>

    </div>
  </div>

  <div>
    <h5>
      Quotes:
    </h5>
  </div>

  <div class="bordered_wrapper">
    <% @book.quotes.each do |quo| %>
      <div class="quote_wrapper">
        <%= quo.description %><br/>
        <%= quo.who_said_it %>
      </div>
    <% end %>
  </div>
</div>
